/*jslint nomen: true */
/*global angular: false, btoa: false, console: false */

angular.module('SETTER')
    .controller('LandingController', [
        function (
        ) {
            'use strict';
            angular.noop();
        }]);
