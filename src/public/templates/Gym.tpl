<div class="btn_black"
    ng-click="back()">
    <i class="icon-left-big"> </i>Back
</div>


<div class="title">{{gym.name}}</div>
<div>{{gym.address}}</div>

<div class="btn_black"
        ng-click="navigateToWalls(gymId)">
    Walls
</div>
